node_modules/
data/
.worktrees/
*.db
*.db-*
.env
test-results/
playwright-report/

# Internal planning docs, not part of the shipped project
docs/superpowers/

# Local Claude Code settings and session artifacts (never shared/committed),
# except .claude/skills/gate/ which is the SKILL.md Gate ships into other projects.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/gate/
.claude-plans/
CLAUDE.local.md

# Local OpenCode configuration (only the shipped Gate skill is committed), the
# native equivalent of .claude/skills/gate/SKILL.md for OpenCode agents.
.opencode/*
!.opencode/skills/
.opencode/skills/*
!.opencode/skills/gate/

# Local agent orientation for working on Gate's own source (opencode, Codex, local
# models) — not the .claude/skills/gate/SKILL.md Gate ships into OTHER projects
AGENTS.md

# Jekyll site build output (built by GitHub Actions, not committed)
site/_site/
site/.jekyll-cache/

# Gate local project state (not shared by default)
.gate/
