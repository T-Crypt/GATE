node_modules/
data/
.worktrees/
*.db
*.db-*
.env
test-results/
playwright-report/

# Internal planning docs, not part of the shipped project
docs/superpowers/

# Local Claude Code settings and session artifacts (never shared/committed)
.claude/
.claude-plans/
CLAUDE.local.md

# Local agent orientation for working on Gate's own source (opencode, Codex, local
# models) — not the .claude/skills/gate/SKILL.md Gate ships into OTHER projects
AGENTS.md

# Jekyll site build output (built by GitHub Actions, not committed)
site/_site/
site/.jekyll-cache/
